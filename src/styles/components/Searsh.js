import { createUseStyles } from 'react-jss';

const SearshStyle = () => ({
  input: {
    width: '400px',
    height: '30px',
    borderStyle: 'none',
    borderRadius: '3px',
    fontSize: '12px',
    paddingLeft: '5px',
  },
});

export default createUseStyles(SearshStyle);
